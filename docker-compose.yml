version: '2'
services:
  app:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/docker-sinatra
    environment:
      - RACK_ENV=development
    command: ["./start_service.sh"]
