version: '2'
services:
  app:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/docker-sinatra
    environment:
      - RACK_ENV=development
    command: ["bundle", "exec", "rackup", "config.ru", "-p", "80", "-s", "thin", "-o", "0.0.0.0"]
